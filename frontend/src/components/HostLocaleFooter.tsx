/* eslint-disable react-refresh/only-export-components */
import styles from "../components/HostLocaleFooter.module.css";

export default function ({ children }) {
  return <footer className={styles.footer}> {children}</footer>;
}
